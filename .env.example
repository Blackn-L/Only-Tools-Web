# CORS proxy base URL — appended with the encoded target URL.
# Leave empty for direct requests (same-origin or local dev).
# Example: VITE_API_PROXY_URL=https://only-tools-web.blackn.workers.dev?target=
